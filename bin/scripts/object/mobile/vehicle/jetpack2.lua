jetpack2 = jetpack2:new {
  templateType = VEHICLE,
	decayRate = 15, -- Damage tick per decay cycle
	decayCycle = 600 -- Time in seconds per cycle
}
ObjectTemplates:addTemplate(jetpack2, "object/mobile/vehicle/jetpack2.iff")
